#include "stdafx.h"

namespace Item {
int add_item(dbproto::PlayerData* playerdata, int itemid, int itemcount) {
    LOG_LOG("add item");
    return 0;
    //Argpack args;
    //args << item << itemcount;
    //SCRIPT("Item.add_item", args);
}
};
